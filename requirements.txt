# ---------- Core scraping / downloading ----------
beautifulsoup4
requests
urllib3
feedparser
curl_cffi

# ---------- Timezone ----------
pytz
tzdata

# ---------- PDF text extraction (ingest_engine.py) ----------
pypdf
PyMuPDF
pdfplumber

# ---------- Database ----------
supabase>=2.0.0

# ---------- Google Drive uploader (main.py) ----------
google-api-python-client
google-auth
google-auth-httplib2
google-auth-oauthlib

# ---------- Gemini ----------
# NOTE: google-generativeai (old SDK, `google.generativeai`) is NOT used.
# The code does `from google import genai`, which ships with google-genai.
google-genai>=1.0.0

# ---------- Telegram bot ----------
# NOTE: the bot uses python-telegram-bot (`telegram` / `telegram.ext`),
# NOT pyTelegramBotAPI (`telebot`). httpx backs telegram.request.HTTPXRequest.
python-telegram-bot>=20.0
httpx

# ---------- Environment / config ----------
python-dotenv

# ---------- MCP server (newsroom_mcp.py) ----------
# MUST stay <2. mcp 2.x removed `mcp.server.fastmcp` and renamed FastMCP to
# MCPServer (mcp.server.mcpserver); the shim left behind raises
# ModuleNotFoundError with a migration pointer, so an unpinned install breaks
# `python newsroom_mcp.py` outright on a fresh CI runner. Migrating to MCPServer
# is a separate change — see the note in newsroom_mcp.create_server().
mcp<2
uvicorn
